"use server";

// import { gatewayService } from "@/lib/services/gateway";

// export async function uploadPdf(formData: FormData) {
//     const response: Response = await gatewayService("/ocr/bound-boxing", {
//         method: "POST",
//         body: formData
//     });
//     return response;
// }

// export async function inferenceOcr(formData: FormData): Promise<unknown> {
//     console.log(formData);
//     const response: InvoicesResponse = await gatewayService("/ocr/inference", {
//         method: "POST",
//         body: formData
//     });
//     return response;
// }