from .baseModel import BaseModel
from .columns import StringColumn, TextColumn

# class Document(BaseModel):
#     name: str = StringColumn(length=255, nullable=False)
#     path: str = TextColumn(nullable=False)